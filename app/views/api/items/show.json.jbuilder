# Need to somehow turn json.items into an array so merge works properly

# json.set! "#{@merchant.id}" do
#   json.extract! @merchant, :id, :store_name
#   json.items json.partial! "api/items/item", item: @item
# end
